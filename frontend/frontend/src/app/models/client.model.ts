export interface Client {
  clientName: string;
  // Add other fields that match your ClientData class
}

export interface ClientForm {
  clientName: string;
  // Add other form fields that match your ClientForm class
} 